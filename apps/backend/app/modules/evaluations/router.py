"""Router raiz del modulo evaluations.

Tarea 3: scaffolding vacio. Los sub-routers (courses, evaluations,
comments, reactions, reports) se montan en la Tarea 12.
"""
from fastapi import APIRouter

router = APIRouter()
